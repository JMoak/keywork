# keywork: Implementation Task Breakdown (superseded)

> **Pointer.** The live task list is [`backlog/`](backlog/README.md); start at its README,
> which indexes the workstream files and the authoritative overlays. Nothing here is canonical.

This file was the first implementation breakdown, written 2026-08-09 from
[`vision.md`](vision.md): milestones M0–M3 plus P2, six parallel workstreams (A engine, B
sessions, C TUI/keyboard, D extensions, E trust, F code-intel) with dependency gates, a per-task
lift strategy (`LIFT:pi` / `LIFT:opencode` / `LIFT:aider` / `OWN`; the former `REIMPL:crush` tag was
retired 2026-08-10), and the late M3 gate for the API-key-only Anthropic provider (G1). Every row
it carried (M0.1–M0.4, A1–A6, B1–B4, C1–C8, D1–D6, E1–E4, F1–F3, G1, and the P2 chain) was
absorbed into `backlog/` at finer grain under the same workstream letters; the tiling acceptance
bar it set for C4 now lives in `backlog/30-tui.md` C8–C11. Its coarse IDs remain superseded
narrative, as `backlog/README.md` states.

*Shrunk to this pointer 2026-08-22 (backlog 111, D-04).*
